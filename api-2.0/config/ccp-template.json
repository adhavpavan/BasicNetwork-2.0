{
    "name": "asset-authe-${ORG}",
    "version": "1.0.0",
    "client": {
        "organization": "${ORG}",
        "connection": {
            "timeout": {
                "peer": {
                    "endorser": "300"
                }
            }
        }
    },
    "organizations": {
        "${ORG}": {
            "mspid": "${ORG}MSP",
            "peers": [
                "peer0.${ORG}.assetauth.vn",
                "peer1.${ORG}.assetauth.vn"
            ],
            "certificateAuthorities": [
                "ca.${ORG}.assetauth.vn"
            ]
        }
    },
    "peers": {
        "peer0.org${ORG}.assetauth.vn": {
            "url": "grpcs://localhost:${P0PORT}",
            "tlsCACerts": {
                "pem": "${PEERPEM}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer0.${ORG}.assetauth.vn",
                "hostnameOverride": "peer0.${ORG}.assetauth.vn"
            }
        },
	"peer1.${ORG}.assetauth.vn": {
            "url": "grpcs://localhost:${P0PORT1}",
            "tlsCACerts": {
                "pem": "${PEERPEM1}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer1.${ORG}.assetauth.vn",
                "hostnameOverride": "peer1.${ORG}.assetauth.vn"
            }
        }

	
    },
    "certificateAuthorities": {
        "ca.${ORG}.assetauth.vn": {
            "url": "https://localhost:${CAPORT}",
            "caName": "ca.${ORG}.assetauth.vn",
            "tlsCACerts": {
                "pem": "${CAPEM}"
            },
            "httpOptions": {
                "verify": false
            }
        }
    }
}
